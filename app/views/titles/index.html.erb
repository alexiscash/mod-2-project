
<!DOCTYPE html>
<html>
    <head>
        
        <style>
            * {
                background: gray;
                font-family: arial, sans-serif;
                color: white;
            }

            body {
                background-image: url("background.jpg");
            }

            /* Add a black background color to the top navigation */
            .topnav {
                background-color: #333;
                overflow: hidden;
                text-align: center;
            }

            /* Style the links inside the navigation bar */
            .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
            }

            /* Change the color of links on hover */
            .topnav a:hover {
            background-color: #ddd;
            color: black;
            }

            /* Add a color to the active/current link */
            .topnav a.active {
            background-color: #4CAF50;
            color: white;
            }
        </style>
    </head>
    <body>
        <%= stylesheet_link_tag '/Users/flatironstudent/Desktop/projects/mod-2-project/app/assets/stylesheets/titles.scss' %>
        <div class="topnav">
        <a class="active" href="titles">Titles</a>
        <a href="genres">Genres</a>
        <a href="users">Users</a>
        <a href="www.google.com">G-man</a>
        </div>
        <h2>all da titles</h2>

        <% if flash[:errors] %>
            <li><%= flash[:errors] %></li>
        <% end %>

        <%= form_with(url: "titles/search", method: "get") do %>
          <%= label_tag(:q, "Search for title:") %>
          <%= text_field_tag(:q) %>
          <%= submit_tag("Search") %>
        <% end %>
        <br><br>
        <% @titles.each do |t| %>
            <li><%= link_to t.name, title_path(t) %></li>
            <br>
        <% end %>
    </body>
</html>