<!DOCTYPE html>
<html>
    <head>
        <style>
            * {
                background: gray;
                font-family: arial, sans-serif;
                color: white;
            }
        </style>
    </head>
    <body>
        <% if flash[:errors]%>
            <% flash[:errors].each do |err| %>
                <li><%= err %></li>
            <% end %>
        <% end %>

        <%= form_for @genre do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.submit %>
        <% end %>
    </body>
</html>