<!DOCTYPE html>
<html>
    <head>
        <style>
            * {
                background: gray;
                font-family: arial, sans-serif;
                color: white;
            }
        </style>
    </head>
    <body>
        <h2><%= @genre.name %> movies</h2>
        <% @titles.each do |t| %>
            <li><%= link_to t.name, title_path(t) %></li>
            <br>
        <% end %>
    </body>
</html>